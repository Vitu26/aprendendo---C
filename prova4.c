#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int i, soma, maior_idade, menor_idade, vet[100] = 0;
	for(i=0;i<=100;i++){
		printf("Informe uma idade: \n"){
		scanf("%d",&idade);
		if(idade!=0){
			soma += idade;
		}else{
			printf("IDADE INVALIDA, ENCERRANDO O PROGRAMA!");
		}
		}
	}
	return 0;
}